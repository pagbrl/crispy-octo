apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: test-ingress-app
  annotations:
spec:
  ingressClassName: nginx
  rules:
  - http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: app
            port:
              number: 3000